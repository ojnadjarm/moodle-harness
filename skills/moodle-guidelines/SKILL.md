---
name: moodle-guidelines
description: Load the full Moodle harness context on demand — guidelines, tips and coding style — when the SessionStart hook did not detect a Moodle project (plugin outside the cwd, unusual layout) or after context was lost. Use when the user asks to load the Moodle guidelines, rules or harness context.
---

# Moodle harness context

These rules are mandatory for the rest of the session. When code is complete, run `/moodle-ci`.

## Guidelines

!`cat "$HOME/.claude/moodle-guidelines.md"`

## Tips

!`cat "$HOME/.claude/moodle-tips.md"`

## Coding style

!`cat "$HOME/.claude/moodle-coding-style.md"`
